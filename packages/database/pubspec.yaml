name: database
description: Drift SQLite database, table definitions and seeder for Ringo
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.38.5"

dependencies:
  bcrypt: ^1.2.0
  drift: ^2.30.0
  drift_flutter: ^0.2.8
  flutter:
    sdk: flutter
  ui_kit:
    path: ../ui_kit
  uuid: ^4.5.1

dev_dependencies:
  build_runner: ^2.4.7
  drift_dev: ^2.30.0
  flutter_test:
    sdk: flutter
  sqlite3: ^2.4.6
