name: observer
description: Talker-backed BlocObserver for Ringo
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.38.5"

dependencies:
  bloc: ^9.0.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  talker: ^5.1.9

dev_dependencies:
  flutter_test:
    sdk: flutter
