name: feature_flags
description: Subscription tier and feature-flag gating (free vs premium) for Ringo
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.38.5"

dependencies:
  config:
    path: ../config
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  utils:
    path: ../utils

dev_dependencies:
  bloc_test: ^10.0.0
  flutter_test:
    sdk: flutter
