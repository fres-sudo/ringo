name: ring_protocol
description: Pure Dart COLMI QRing packet codec, device profiles, and typed commands for Ringo.
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"

dev_dependencies:
  test: ^1.26.3
