name: storage_tostore
description: Domain-neutral local storage API backed by ToStore for Ringo.
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  tostore: ^3.1.2

dev_dependencies:
  test: ^1.24.0
