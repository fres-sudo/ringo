name: printing
description: Receipt building, ESC/POS rendering and thermal printing for Ringo
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.38.5"

dependencies:
  esc_pos_utils_plus: ^2.0.4
  flutter:
    sdk: flutter
  result:
    path: ../result
  talker: ^5.1.9

dev_dependencies:
  flutter_test:
    sdk: flutter
